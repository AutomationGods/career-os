# System Kernel Domain

Layer: Platform

This domain is registered in the Domain Registry and must follow Domain → Manager → Capability → Worker → Tool → Event → State Projection.

Runtime Audit v1 uses `system.runtime_audit` to report which registered domains, commands, managers, tools, events, and state projections are actually runtime-wired.
